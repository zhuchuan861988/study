{
  "name": "auto-deploy-tool",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . iobit自动部署工具 --platform=win32 --arch=x64 --out=release --overwrite --icon=icon.ico"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^26.8.1",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "basic-ftp": "^5.0.4",
    "electron-store": "^8.1.0",
    "simple-git": "^3.22.0",
    "ssh2-sftp-client": "^8.0.0"
  }
}
